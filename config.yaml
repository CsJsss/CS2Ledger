# CS2 Ledger configuration
# All fields are optional — remove a key to use the default value.

log:
  level: info # debug | info | warn | error
  # path: "logs/app.log" # if set, logs are written to this file (as well as stderr)

# db:
#   dsn: "file:data.db"
#   wal_mode: true
#   busy_timeout: 5000
